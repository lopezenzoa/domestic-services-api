package com.portfolio.domestic_services.model;

public enum States {
    // REQUESTING: the call was not confirmed yet by the provider
    // PENDING: the call was confirmed by the provider
    FINISHED, PENDING, DECLINED, REQUESTING
}
